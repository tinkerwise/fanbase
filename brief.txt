**Maintainer Brief**

Yard Report is a single-page Astro site with a thin static shell and a very thick client script. The core implementation lives in [app.js](/Users/briancsmith/Documents/GitHub/yardreport/src/scripts/app.js), which owns state, data fetching, rendering, event wiring, settings, and Easter eggs. The page markup in [index.astro](/Users/briancsmith/Documents/GitHub/yardreport/src/pages/index.astro) is mostly stable scaffolding, and [style.css](/Users/briancsmith/Documents/GitHub/yardreport/public/style.css) carries nearly all layout and component styling. `FEATURES.txt` is effectively the product backlog and prompt contract.

**What’s Fragile**

The biggest risk is the “all roads lead to one file” setup in [app.js](/Users/briancsmith/Documents/GitHub/yardreport/src/scripts/app.js). Rendering and behavior are tightly coupled, so small UI changes can easily break event handlers, indexing, or filter state. A few especially sensitive areas:
- `renderArticles()` and `renderCard()` drive most feed UX; changes here can break reader opening, read/unread state, bundles, or swipe behavior.
- `getFilteredArticles()` is the source of truth for what appears; filter bugs will ripple everywhere.
- `setupEvents()` is large and centralized; new controls should be added carefully to avoid duplicated listeners or state drift.
- Score/on-deck/transactions/leaders all depend on third-party APIs with no formal abstraction layer, so breakage from upstream response changes is a real risk.
- There is both a dev-time JS proxy in [astro.config.mjs](/Users/briancsmith/Documents/GitHub/yardreport/astro.config.mjs) and a production PHP proxy in [rss-proxy.php](/Users/briancsmith/Documents/GitHub/yardreport/public/rss-proxy.php); feed issues may differ between local and deployed environments.

**How I’d Approach Future Prompts**

For feature work, I’d first check whether it’s already described in [FEATURES.txt](/Users/briancsmith/Documents/GitHub/yardreport/FEATURES.txt), then map it to one of three surfaces:
- structure change: [index.astro](/Users/briancsmith/Documents/GitHub/yardreport/src/pages/index.astro)
- behavior/data change: [app.js](/Users/briancsmith/Documents/GitHub/yardreport/src/scripts/app.js)
- visual/responsive change: [style.css](/Users/briancsmith/Documents/GitHub/yardreport/public/style.css)

For safety, I’d usually preserve:
- existing filters, reader behavior, and localStorage prefs
- mobile-first constraints and content density rules
- Orioles-first prioritization in scores, schedule, and sidebar widgets

**Best Next Refactors**

If we want the repo easier to maintain without changing behavior, the highest-value cleanup would be:
- split [app.js](/Users/briancsmith/Documents/GitHub/yardreport/src/scripts/app.js) into modules by domain: feeds, scores, sidebars, reader, settings
- separate pure render helpers from fetch/state logic
- centralize API wrappers and response shaping
- add a small smoke-test checklist for feed load, filters, reader, scores, and settings persistence

**Prompting Guidance**

The prompts that will work best with this repo are specific and scoped, like:
- “Add X to the feed toolbar without changing current mobile behavior”
- “Refine the On Deck card to show Y and keep the current visual style”
- “Implement the next unchecked item from `FEATURES.txt`”
- “Refactor just the leaders widget into a separate module without changing output”

The prompts most likely to cause churn are broad redesigns or “clean this all up” requests, because the codebase is functional but tightly intertwined.